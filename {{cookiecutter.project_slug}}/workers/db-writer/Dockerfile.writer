FROM node:{{ cookiecutter.node_version }}-alpine

WORKDIR /app

# Frozen install against the writer's own lockfile.
COPY workers/db-writer/package.json workers/db-writer/package-lock.json ./
RUN npm ci --omit=dev

COPY workers/db-writer/worker.mjs ./worker.mjs

CMD ["node", "worker.mjs"]
